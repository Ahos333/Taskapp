export class Task {
    id: number;
    name: string;
}

/*
This project only needed one Model: Task, and each task will have an id (numerical value like 1), 2), etc.) and a name
(or the task/duty itself)
*/